apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: podinfo
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/alexacid88/what-time-is-it
  ref:
    branch: master
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/chart